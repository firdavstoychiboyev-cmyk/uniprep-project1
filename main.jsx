import React from 'react'
import ReactDOM from 'react-dom/client'
import App from './uniprep_ver_.jsx' // Убедись, что имя файла совпадает!

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
)
